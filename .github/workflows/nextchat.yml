name: Deploy NextChat to Cloudflare Pages

on:
  schedule:
    - cron: '0 0 * * 1'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          repository: 'ChatGPTNextWeb/NextChat'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18 
          cache: 'yarn'

      - name: Install Dependencies
        run: yarn install --frozen-lockfile

      - name: Build Static Export
        run: npx cross-env NEXT_DISABLE_ESLINT=1 BUILD_MODE=export BUILD_APP=1 yarn mask && npx cross-env NEXT_DISABLE_ESLINT=1 BUILD_MODE=export BUILD_APP=1 next build
        env:
          CODE: ${{ secrets.CODE }}

      - name: Publish to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy ./out --project-name=ai --branch=main
